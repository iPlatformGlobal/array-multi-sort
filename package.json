{
  "name": "@iplatformglobal/array-milti-sort",
  "version": "1.0.0",
  "description": "Array helper for soring array object on multiple key values with ordering",
  "main": "build/index.js",
  "scripts": {
    "preversion": "yarn test",
    "postversion": "git push && git push --tags && npm publish",
    "prepare": "yarn build",
    "build": "webpack --progress --profile --bail",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "lint": "eslint --ext .jsx --ext .js lib/",
    "commitmsg": "validate-commit-msg",
    "prepush": "yarn lint && yarn test",
    "postmerge": "yarn"
  },
  "publishConfig": {
    "registry": "https://nexus.iplatform.co.za/repository/iPlatformNPM/"
  },
  "author": "Reme Le Hane <reme@iplatform.com> (https://www.remelehane.me)",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.25.0",
    "babel-eslint": "^7.2.3",
    "babel-jest": "^20.0.3",
    "babel-loader": "^7.1.2",
    "babel-plugin-root-import": "^5.1.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-1": "^6.24.1",
    "eslint": "^4.4.0",
    "husky": "^0.14.3",
    "jest": "^21.0.1",
    "path": "^0.12.7",
    "validate-commit-msg": "^2.14.0",
    "webpack": "^3.5.5"
  },
  "jest": {
    "roots": [
      "<rootDir>/lib"
    ],
    "testResultsProcessor": "./node_modules/jest-bamboo-formatter",
    "testPathIgnorePatterns": [
      "<rootDir>/tests"
    ]
  },
  "dependencies": {
    "jest-bamboo-formatter": "^1.0.1"
  }
}
